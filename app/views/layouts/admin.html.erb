<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <%= csrf_meta_tags %>

    <link rel="shortcut icon" href="/favicon.png">

    <title><% if @blog %><%= @blog.name %>: <% end %><%= t('admin.admin interface') %><% if content_for?(:title) %>: <%= yield :title %><% end %></title>

    <%= stylesheet_link_tag :admin, "data-turbolinks-track" => true %>

    <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
    <%= javascript_include_tag "admin" %>
  </head>

  <body>
    <div class="navbar navbar-fixed-top navbar-inverse" role="navigation">
      <div class="container">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
            <span class="sr-only"><%= t 'admin.toggle navigation' %></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <%= link_to 'Burp', 'https:/github.com/ckruse/Burp', class: "navbar-brand" %>
        </div>
        <div class="collapse navbar-collapse">
          <ul class="nav navbar-nav">
            <li class="active"><%= link_to((@blog ? @blog.name : 'Burp') +  ' ' + t('admin.admin interface'), admin_path) %></li>
            <li><%= link_to t('admin.configuration'), '' %></li>
            <li><%= link_to t('admin.posts'), '' %></li>
            <li><%= link_to t('admin.comments'), '' %></li>
            <li><%= link_to t('admin.media'), '' %></li>
          </ul>

          <ul class="nav navbar-nav navbar-right">
            <li><%= link_to(t('global.logout'), destroy_author_session_path, :method => :delete) %></li>
          </ul>
        </div><!-- /.nav-collapse -->
      </div><!-- /.container -->
    </div><!-- /.navbar -->

    <div class="max-width-fluid">
      <div class="row row-offcanvas row-offcanvas-right">

        <%= render 'layouts/admin_sidebar' %>

        <div class="col-xs-12 col-sm-<%= @has_subnav ? 10 : 12 %>">
          <div id="alerts-container">
            <% flash.each do |name,msg| %>
              <div class="alert alert-<%= name == :notice ? "success" : "error" %> fade in">
                <button type="button" class="close" data-dismiss="alert">×</button>
                <%= msg %>
              </div>
            <% end %>
          </div>



          <%= yield %>

        </div><!--/div col-xs-12-->
      </div><!--/row-->
    </div><!--/.container-->

  </body>
</html>
