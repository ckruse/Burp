<% content_for :title do %><%= t('admin.configuration') %><% end %>

<h2><%= t('admin.configuration') %></h2>

<%= form_for @blog, url: admin_blog_path do |f| %>

  <%= render 'form', f: f %>

  <%= f.submit t('admin.save'), class: 'btn btn-primary' %>
  <%= link_to t('admin.cancel'), admin_comments_path, class: 'btn btn-default' %>

<% end %>
