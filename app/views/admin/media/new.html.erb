<h2>New Medium</h2>

<%= form_for @medium, url: admin_media_path do |f| %>
  <%= render 'form', f: f %>

  <p>
    <%= f.submit 'Save', class: 'btn btn-primary' %>
    <%= link_to 'Cancel', admin_posts_path, class: 'btn btn-default' %>
  </p>

<% end %>
